<% slides.each_with_index do |slide, index| %>
  <div class="hero-slide <%= 'active' if index.zero? %>" data-hero-carousel-target="slide" data-label="<%= slide['label'] %>" aria-hidden="<%= !index.zero? %>"><img src="<%= slide['image'] %>" alt="<%= slide['alt'] %>"></div>
<% end %>
